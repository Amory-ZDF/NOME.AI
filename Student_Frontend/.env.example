# When VITE_API_BASE_URL is set, the app talks to a real backend
# over the REST endpoints documented in API_INTERFACE.md.
# Leave it empty/unset to run in frontend-only mock mode.
# VITE_API_BASE_URL=https://api.nome.ai
